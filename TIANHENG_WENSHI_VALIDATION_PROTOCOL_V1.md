# 天衡・九維命理｜問事驗證規格 v1

## 目的

問事引擎不能以「文字聽起來合理」當成有效。驗證必須分開回答三件事：

1. 規則是否依版本穩定重現。
2. 在不知道結果時，方向與時間窗口是否命中。
3. 引擎是否知道何時證據不足、應該保留而不硬斷。

## 三層驗證

### A. 歷史盲測

- 只採用可確認原始提問時間、起局資料及結果來源的舊事件。
- 操作者只取得去除結果的 `blindView`。
- 預測寫入並鎖定後，才揭露事件結果。
- 歷史事件可以早於本次判讀，但必須記錄案例凍結時間、預測鎖定時間及揭盲時間，並證明操作者沒有答案存取權。
- 若只有事件結果、沒有當時問題或起局資料，只能列為「回顧說明」，不得計入準確率。

### B. 保留樣本

- 規則調整使用訓練案例；另保留一批從未參與調參的案例。
- 每次演算法改版，先凍結版本，再跑保留樣本。
- 不可因個別失敗案例臨時增設只對該例有效的規則。

### C. 前瞻驗證

- 使用者提問時立即保存問題、起局資料、引擎版本、完整預測與時間窗口。
- 在結果揭曉前不可修改預測；更正只能新增版本並保留原稿。
- 結果由可查證來源回填，未揭曉者保持 `unresolved`。

## 核心指標

| 指標 | 用途 | 防止的假象 |
|---|---|---|
| directionAccuracy | 正／負／混合方向命中率 | 只挑說中的案例 |
| coverage | 實際給出可判斷結論的比例 | 大量拒答換取高命中率 |
| timingAccuracy | 預測窗口與事件窗口是否重疊 | 只說「以後會發生」 |
| brierScore | 機率信心是否校準，越低越好 | 每次都說很有把握 |
| evidenceCompleteness | 支持與阻力是否同時留下 | 只留有利證據 |

## 資料洩漏紅線

- 前瞻與保留測試的 `prediction.createdAt` 必須早於 `actual.knownAt`。
- 歷史盲測不要求晚近預測早於舊事件，但必須在預測前封存案例、隔離答案，並在預測鎖定後才揭盲。
- 案例輸入不得含結果提示、事後評論或由結果反推的分類。
- 演算法版本、曆法版本、時區與起局規則必須可重現。

## 現階段結論

問事 v1 採用三枚銅錢六爻法：每次三枚、共六次，依初爻至上爻輸入；6 為老陰、7 為少陽、8 為少陰、9 為老陽。原始銅錢值、主卦、動爻、變卦與後續裁決分層保存。

目前建立的是起卦與驗證基礎設施，不代表六爻裁決已通過實證。正式準確率必須等用神、日月旺衰、動變生剋、沖合與應期規則鎖定，並完成歷史盲測案例集後才能計算。
