/* 天衡・九維命理｜四階十年時間線完整講解 v4.0 */
(function(root){'use strict';
function uniq(xs){return xs.filter(function(x,i,a){return x&&a.indexOf(x)===i;});}
function clean(x){return String(x||'').replace(/[。；]+$/,'');}
function explain(r){if(!r||!r.years||r.years.length!==10)throw Error('需要完整十年時間線');var segs=r.dayunSegments.map(function(x){return {label:x.startYear+'–'+x.endYear+'｜'+x.gz+'大運',text:'格局由本命進入「'+x.variant+'」，大運用神狀態為「'+x.status+'」，作用分 '+x.score+'。',reading:'轉態：'+(x.transitions.join('、')||'延續')+'。本段先定十年底盤，逐年仍須另看流年是否加強、救應或消耗。'};}),years=r.years.map(function(x){return {label:x.year+' '+x.gz+'｜'+x.category,text:x.reading,reading:'主題：'+x.focus.key+'・'+x.focus.theme+'。可以做：'+x.actions.slice(0,2).map(clean).join('；')+'。先避免：'+x.avoid.slice(0,2).map(clean).join('；')+'。證據：'+x.evidence.map(clean).join('、')};}),details=segs.concat(years),first=r.dayunSegments[0],why='時間線從 '+r.window.startYear+' 到 '+r.window.endYear+'，先以 '+first.gz+' 大運建立底盤，再把每一年獨立加入命局，重新計算格局轉態、第一用神得失、宮位沖合與生活領域。'+(r.dayunSegments.length>1?'這十年中途會換入 '+r.dayunSegments[1].gz+' 大運，因此換運前後分段保存，沒有把十年誤當成同一運。':'本窗口均落在同一步大運，但十個流年仍逐年重算。'),impact='十年內有 '+r.overview.supportCount+' 年的綜合作用分達支持區，'+r.overview.pressureCount+' 年低於零。支持窗口為 '+r.supportWindows.join('、')+'；壓力窗口為 '+r.pressureWindows.join('、')+'。這些年份表示承接條件的相對高低，不等同必然發生好事或壞事。',actions=['把支持窗口用於推進一至兩項已有準備的長期目標，不同時開太多戰線。','壓力窗口提前保留現金、時間與替代方案，把不可逆決定拆成試行與正式兩階段。','遇到換運年份，先觀察角色、責任與生活節奏如何改變，再調整原計畫。','每年仍以當年度真實工作、關係與財務資料覆核，不只依命理分數。'],avoid=['把分數最高年當成保證成功，或把低分年視為一定出事。','只看流年而忽略大運底盤，也不要用大運結論覆蓋本命用神。','財務支持年仍不保證投資獲利，避免因命理結果放大槓桿。'],evidence=['十年主軸：'+r.overview.mainAxis,'第一用神：'+r.overview.primaryElement,'大運分段：'+r.overview.segmentCount].concat(r.turningPoints.slice(0,8).map(function(x){return x.year+' '+x.gz+'・'+x.type;}));return {title:r.window.startYear+'–'+r.window.endYear+'・大運十年主軸與逐年轉折',summary:'這不是把十個年份各寫一句吉凶，而是先判大運底盤，再逐年比較用神得失、格局改變與宮位作用，找出真正需要推進、修復或換方法的年份。',why:why,impact:impact,actions:actions,avoid:avoid,evidence:uniq(evidence),details:details,generatedFromEvidence:true,legacyOverride:false,disclaimer:r.disclaimer};}
function safeExplain(r){try{return {ok:true,result:explain(r),error:null};}catch(e){return {ok:false,result:null,error:String(e&&e.message||e)};}}
var api=Object.freeze({explain:explain,safeExplain:safeExplain});if(!root.TianhengBaziDecadeExplanationV4)root.TianhengBaziDecadeExplanationV4=api;if(typeof module!=='undefined'&&module.exports)module.exports=api;
})(typeof window!=='undefined'?window:globalThis);
